# teinvit-project
TeInvit – WordPress plugin (teinvit-core) + Node.js Puppeteer PDF service (teinvit-pdf).  JS-first invitation rendering (/preview, /i/{token}, /pdf/{token}) with final PDF generated via Puppeteer.  Production-mirrored structure.
